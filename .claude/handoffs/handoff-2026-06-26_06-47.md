# Handoff — 2026-06-26 06:47 UTC

## Objectif de la session

(1) Produire deux documents de conception : `docs/backend-design.md` (NestJS séparé, REST, cookies HttpOnly, JWT access+refresh, TypeORM, storage dual Supabase/MinIO) et `docs/frontend-update.md` (Next.js 15+ + shadcn/ui adoption sélective, theming par catégorie, age gate, standards UX formulaires). (2) Lancer ensuite l'implémentation côté front : migration Next.js 14 → 16, init shadcn/ui, primitives P1, refactor des composants existants vers shadcn (AgeGate, Navbar mobile, LoginForm, CommandeForm…).

## Tentatives échouées

| # | Approche | Cause de l'échec |
|---|----------|------------------|
| 1 | Lancer les refactors D1 (`AgeGate`) et D2 (`Navbar`) en parallèle via deux `implementer` simultanés | Les deux implementers ont écrit sur le même working tree non commité ; chaque `validator` voyait les deux diffs et signalait "fichier hors périmètre" pour la tâche jumelle. **Résolution** : commit séparé après vérification fonctionnelle ; les deux refactors étaient corrects au fond, gates verts. Pour la suite, exécution **séquentielle** (commit après chaque tâche) avant nouvelle délégation. |
| 2 | Première passe Phase A — `app/produits/page.tsx` non migré | L'implementer a couvert 3 pages avec params/searchParams Promise mais a oublié `app/produits/page.tsx`. Détecté par `validator`. Corrigé en une passe ciblée. |
| 3 | Délégation Phase D3 (LoginForm) à un `implementer` | Agent a atteint la limite de session quota au lancement. **Résolution** : refactor exécuté inline par l'orchestrateur (logique simple, tests existants compatibles avec shadcn Form). |
| 4 | `pnpm dlx shadcn@latest init` en mode non-interactif | Init officiel demande des réponses interactives. **Résolution** : implementer Phase B a créé les artefacts manuellement (`components.json`, `lib/utils.ts cn()`, HSL vars, tailwind.config.ts) en respectant la spec shadcn new-york. Validator a PASS. |

## Tentatives réussies

| # | Approche | Pourquoi ça a marché |
|---|----------|----------------------|
| 1 | Cycle de questions ciblées via `AskUserQuestion` avant chaque réécriture de doc (8 décisions architecturales au total — sous-cat obligatoire, palette JSON, lucide icons, age gate cookie, héritage thème, migration auto "Général", scope theming, NestJS owns auth, TypeORM, cookies parent domain, class-validator, TanStack Query, JWT access+refresh, storage dual) | Décisions tranchées en amont → docs cibles cohérentes du premier coup, pas d'allers-retours après écriture. |
| 2 | Phase A — migration Next.js 14.2.35 → 16.2.9 + React 18 → 19 | `next@latest` a résolu en 16 (post-15) : `@next/codemod upgrade latest` + adaptations manuelles (params/searchParams Promise sur 4 pages, `cache: 'force-cache'` explicite, `eslint.config.mjs` flat config ESLint 9 imposé par eslint-config-next 16, désactivation des règles React Compiler). Gates 4/4 verts. Commit `83b7eb6`. |
| 3 | Phase B — Init shadcn + bridge HSL marque | Couleurs de marque (`#ED4C20`, `ink`, `surface-muted`, `graphite`) injectées dans les CSS vars HSL shadcn (`--primary: 14 87% 53%` etc.) → composants shadcn nativement orange Primal **sans** toucher aux classes Tailwind existantes. Tokens marque ET shadcn coexistent dans `tailwind.config.ts`. Commit `c838460`. |
| 4 | Phase C — 17 primitives shadcn + variant `cat` du Button + Toaster Sonner | `pnpm dlx shadcn@latest add … --overwrite` avec `CI=true` ⇒ install non-interactif fonctionnel. Variant `cat: "bg-cat-primary text-cat-on hover:bg-cat-hover"` ajouté dans `cva` du Button (catégorie theming compatible). Toaster monté dans `app/layout.tsx`. Commit `35f2d63`. |
| 5 | D1 — `AgeGate` refactor vers shadcn `Dialog` | Markup `<div fixed inset-0>` + boutons natifs remplacé par `Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription/DialogFooter + Button × 2`. Logique 100% préservée (localStorage `primal-age-confirmed`, `useConfirmAge`, gate `isAdult`, `router.push('/categories')` sur close). Focus trap + ESC + scroll lock natifs Radix. Commit `8ee951b`. |
| 6 | D2 — Navbar mobile drawer vers shadcn `Sheet` | Custom `mobileOpen` useState + panneau conditionnel remplacé par `Sheet/SheetTrigger/SheetContent`, chaque `<Link>` wrappé dans `<SheetClose asChild>` (auto-close à la nav). `aria-label="Ouvrir le menu"` strictement préservé (dépendance e2e). Menu desktop intact. Commit `9dca03e`. |
| 7 | D3 — `LoginForm` refactor inline (post limite agent) | shadcn `Form + FormField + Input + Button + Alert + Loader2 + Sonner` + `valibotResolver(loginSchema)`. Tests existants compatibles sans modification (sélecteurs `getByLabelText` / `getByRole`). Commit `f59d45e`. |
| 8 | D4 — `CommandeForm` refactor inline | Mêmes primitives + `Textarea` + `FormDescription` pour le hint téléphone (masqué quand erreur). Quantité avec `valueAsNumber` géré via `onChange` custom pour éviter NaN sur clear. Logique `useCreateCommande` + `prixAttendu` + redirect `/confirmation` préservée. Tests 26/26 passent sans modification. Commit `1b9f8d9`. |

## Prochaines étapes

- [ ] **D5 — `components/admin/CategoriesManager.tsx`** (469 lignes + 128 lignes test) : migrer vers shadcn `Form + Dialog + AlertDialog + Input + Switch + Sonner`. Créer la modale d'édition (Dialog) + confirmation suppression (AlertDialog). Adapter `CategoriesManager.test.tsx`. Composants additionnels à créer pendant ou avant : `components/admin/ThemePicker.tsx` (Popover + 5 color pickers HEX), `components/admin/IconPicker.tsx` (Command shadcn + grille lucide), `components/admin/CoverUploader.tsx` (drag&drop + mutation upload).
- [ ] **D6 — `components/admin/PromotionsManager.tsx`** (282 lignes) : même pattern que D5, sans pickers. Form + Dialog + AlertDialog + Sonner.
- [ ] **D7 — `components/admin/ProduitForm.tsx`** (433 lignes) : refactor shadcn `Form + Select` avec **cascade catégorie → sous-catégorie**. Section médias (image + vidéo) avec `CoverUploader`-like. Preview live `<ProductCard>` à droite (md+). Cf. doc front v3 §7.1 et §7bis.9.
- [ ] **D8 — `components/admin/CommandesTable.tsx`** (237 lignes) : créer `components/admin/DataTable.tsx` (wrapper shadcn `Table` + `@tanstack/react-table`). Migrer la table de commandes : `Badge` pour statut, `DropdownMenu` pour actions, `Tabs` pour filtre période.
- [ ] **D9 — `components/admin/ProduitsTable.tsx`** (119 lignes) : utiliser le même `DataTable` + `Badge` (actif/inactif, promo).
- [ ] **D10 — nouvelle page `app/admin/sous-categories/`** : créer le CRUD sous-catégories (page + form avec cascade catégorie parent obligatoire). Cf. doc backend v3 §6.5.
- [ ] **Tests Playwright** — lancer enfin la suite scaffolded (`e2e/*.spec.ts`), ajuster sélecteurs (probables après refactor shadcn) : modal AgeGate, Sheet Navbar, FormMessage. Cf. handoff précédent.
- [ ] **Optionnel — codegen via `api-forge`** depuis `openapi/primal-api.openapi.json` (déjà présent à la racine `docs/openapi.json` à confirmer) pour générer types + services + schemas + hooks TanStack Query. Voir doc front v3 §6.
- [ ] **Décider du sort de `/produits`** (catalogue global) : conservé en page, retiré de la navbar principale par handoff précédent.

## Notes additionnelles

### Branche et historique
- Branche : `master-v2`
- Commits ajoutés cette session (8 total) :
  ```
  1b9f8d9 refactor(commande): migrate CommandeForm to shadcn Form + Sonner
  f59d45e refactor(login): migrate LoginForm to shadcn Form + Sonner
  9dca03e refactor(navbar): migrate mobile drawer to shadcn Sheet
  8ee951b refactor(age-gate): migrate AgeGate to shadcn Dialog
  35f2d63 feat(ui): install shadcn primitives and add cat variant to Button
  c838460 chore(ui): init shadcn/ui and bridge HSL tokens to brand colors
  83b7eb6 chore(deps): migrate to Next.js 16 + React 19
  ```
- Gates verts confirmés à chaque commit : `pnpm tsc --noEmit` 0, `pnpm lint` 0, `pnpm test` 26/26, `pnpm build` (22 routes).

### Documents livrés (gitignored — `docs/*.md`)
- `docs/backend-design.md` **v3.0** — conception NestJS standalone, JWT cookies HttpOnly, TypeORM, storage dual (Supabase + MinIO via abstraction `StorageService`), 6 phases roadmap backend.
- `docs/frontend-update.md` **v3.0** — Next.js 15 → 16 migration, adoption shadcn/ui sélective, theming runtime par catégorie (`--cat-*` CSS vars + fallback marque), age gate localStorage, standards UI/UX formulaires (§7bis), mapping composant par composant Garder/Améliorer/Réécrire.

### Stack actuelle (post Phase A-D4)
- `next 16.2.9`, `react 19.2.x`, `typescript ^5`, `tailwindcss 3.4.19`
- `@tanstack/react-query 5.101`, `@tanstack/react-table 8.21.3`
- `axios 1.18`, `react-hook-form 7.79`, `valibot 1.4`, `@hookform/resolvers 5.4`
- `sonner 2.0`, `next-themes 0.4`, `zod ^4` (peer de `Form`)
- `class-variance-authority 0.7`, `clsx 2.1`, `tailwind-merge 3.6`, `tailwindcss-animate 1.0`
- Radix peers shadcn (`@radix-ui/react-{dialog,select,checkbox,switch,dropdown-menu,...}`)
- `lucide-react 0.468`, `recharts 2.14`, `resend 6.12`
- `eslint 9.39`, `eslint-config-next 16.2.9`, `@playwright/test 1.61`, `vitest 4.1`

### Points d'attention pour la suite
- **Exécution séquentielle obligatoire pour la suite Phase D** : éviter le piège des parallel implementers (cf. tentative 1 échouée). Commit après chaque tâche, validator sur diff propre.
- **`tsconfig.json` `jsx: "react-jsx"`** : modifié par Next 16 au premier build. Next supporte officiellement `"preserve"` mais le build passe avec `"react-jsx"`. Surveiller si comportement étrange en SSR.
- **`next lint` retiré dans Next 16** : script `lint` migré vers `eslint . --ext .ts,.tsx`. Pas de régression mais surveiller la perte éventuelle de règles spécifiques Next App Router.
- **shadcn primitives** : toutes en `components/ui/` (17 fichiers). On en est propriétaires (style new-york). Personnalisations possibles localement.
- **Variant `cat` du Button** : `cat: "bg-cat-primary text-cat-on hover:bg-cat-hover"`. Utiliser `<Button variant="cat">` dans les sous-arbres `/categories/[slug]/*` pour theming par catégorie.
- **Token `primary`** : double registre — hex direct (`#ED4C20`) ET HSL via CSS var pour shadcn. Coexistence fonctionnelle (cf. Phase B validator).
- **Tests Playwright** : non lancés cette session. Devraient passer mais sélecteurs `aria-label="Ouvrir le menu"` confirmé préservé après Sheet migration.
- **AgeGate** : reste localStorage (`primal-age-confirmed`) — décision actée handoff précédent ; pas migré vers cookie HttpOnly NestJS pour cette itération.
- **Backend NestJS** : sur `localhost:3001/api/v1` (handoff précédent confirmait UP avec 4 catégories / 16 produits). Pas démarré cette session — front buildé sans backend.

### Composants nouveaux à créer (cf. doc front v3 §3.8)
- `components/admin/ThemePicker.tsx` — Popover + 5 color pickers HEX + preview live + alerte WCAG (helper `lib/theme/contrast.ts`)
- `components/admin/IconPicker.tsx` — Command shadcn + grille `ICON_NAMES` (cf. `@/lib/api/types`)
- `components/admin/CoverUploader.tsx` — drag&drop + mutation upload (Supabase Storage ou MinIO via backend)
- `components/admin/ReorderList.tsx` — `@dnd-kit/sortable` pour ordre `position`
- `components/admin/DataTable.tsx` — wrapper générique `Table` shadcn + `@tanstack/react-table`
- `components/admin/FormSection.tsx`, `FormActions.tsx`, `FieldGroup.tsx` — composables form (cf. §7bis.8)
- `lib/forms/use-server-errors.ts`, `use-dirty-guard.ts`, `slugify.ts`, `format.ts` — helpers UX form
- `lib/icons.ts` — allowlist déjà présente dans `@/lib/api/types` (`ICON_NAMES`) ; reconfirmer / centraliser

### Comment reprendre
```
1. git log --oneline -10                     # vérifier qu'on est sur 1b9f8d9
2. cat docs/frontend-update.md | less        # relire §3.4 (primitives), §5, §7, §7bis
3. cat .claude/handoffs/handoff-2026-06-26_06-47.md
4. Lancer D5 — refactor CategoriesManager (et créer ThemePicker, IconPicker, CoverUploader
   en amont ou pendant)
5. Brief implementer avec périmètre EXPLICITE :
   - components/admin/CategoriesManager.tsx + .test.tsx
   - components/admin/ThemePicker.tsx (nouveau)
   - components/admin/IconPicker.tsx (nouveau)
   - components/admin/CoverUploader.tsx (nouveau)
6. Validator → diff propre (working tree vs HEAD = 1b9f8d9)
7. Commit, puis D6, D7, D8, D9, D10 en séquentiel.
```
